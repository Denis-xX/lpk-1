import * as modFunctions from "./modules/functions.js";

modFunctions.isWebp();

import $ from "jquery";
import "slick-carousel";

  $('.hero-slider').slick({
    arrows: true,
    dots: false,
    fade: true,
    autoplay: false,
  });
